{
    /*
     * Environment specific settings
     */
    "connection" : {
        "host":     "localhost",
        "user":     "admin",
        "password": "password"
    },

    //You can define value placeholders here
    "global-values" : {
        "%%HOSTNAME%%" : "localhost",
        "%%TRIGGERS-DATABASE%%" : "Triggers"
    }
}
